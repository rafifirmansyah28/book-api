/* eslint-disable no-undef */
describe('Sample unit test', () => {
    test('Simple logic works', () => {
        // Example function to test
        const sum = (a, b) => a + b;

        // Test the function
        expect(sum(2, 3)).toBe(5);
    });
});